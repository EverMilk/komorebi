"""Komorebi — an open-source AITuber engine that makes AI approachable.

The Python core does all the thinking (LLM, emotion, TTS, persona, orchestration)
and streams events to a thin browser renderer over a single WebSocket. See
``docs/architecture.md`` for the wire contract.
"""

__version__ = "0.0.1"
